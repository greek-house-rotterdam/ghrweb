---
import Layout from "../../layouts/Layout.astro";
import { t } from "../../i18n/ui";
import { getStaticLangPaths, type Lang } from "../../i18n/utils";

export const getStaticPaths = getStaticLangPaths;

const { lang } = Astro.params as { lang: Lang };
---

<Layout title={t(lang, "nav.contact")} lang={lang}>
  <section class="py-12">
    <div class="mx-auto max-w-7xl px-4">
      <h1 class="text-3xl font-bold text-gray-900">{t(lang, "nav.contact")}</h1>
      <div class="mt-8 grid gap-8 lg:grid-cols-2">
        <!-- Google Form embed -->
        <div>
          <!-- TODO: Replace with actual Google Form embed URL -->
          <iframe
            src="https://docs.google.com/forms/d/e/FORM_ID/viewform?embedded=true"
            width="100%"
            height="600"
            class="rounded-lg border border-gray-200"
            title="Contact Form"
          >
            Loading…
          </iframe>
        </div>
        <!-- Map & contact info -->
        <div>
          <!-- TODO: Replace with actual Google Maps embed -->
          <div class="aspect-video rounded-lg bg-gray-100 flex items-center justify-center">
            <p class="text-gray-400">Google Maps embed</p>
          </div>
          <div class="mt-6 space-y-3 text-sm text-gray-600">
            <p>📍 Greek House in Rotterdam, Address TBD</p>
            <p>📞 Phone TBD</p>
            <p>✉️ email@example.com</p>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>
