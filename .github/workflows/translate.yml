name: Translate Content

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize]
    paths:
      - "src/content/**/*.md"

permissions:
  contents: write

jobs:
  translate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install deepl pyyaml

      - name: Detect changed content files
        id: changes
        run: |
          git fetch origin main
          # Use -z for null-terminated strings to handle spaces/special chars safely
          # We need to process this carefully for the python script arguments
          FILES=$(git diff --name-only --diff-filter=ACM origin/main...HEAD -- 'src/content/*/gr/*.md' 'src/content/*/nl/*.md' 'src/content/*/en/*.md' | tr '\n' ' ')
          
          echo "Detected files: $FILES"
          echo "files=$FILES" >> "$GITHUB_OUTPUT"

      - name: Translate content
        if: steps.changes.outputs.files != ''
        env:
          DEEPL_API_KEY: ${{ secrets.DEEPL_API_KEY }}
        run: python .github/scripts/translate.py ${{ steps.changes.outputs.files }}

      - name: Sync deletions across languages
        run: |
          git fetch origin main
          DELETED=$(git diff --name-only --diff-filter=D origin/main...HEAD -- 'src/content/*/gr/*.md' 'src/content/*/nl/*.md' 'src/content/*/en/*.md' | tr '\n' ' ')
          
          if [ -z "$DELETED" ]; then
            echo "No content files deleted."
          else
            echo "Deleted files: $DELETED"
            python .github/scripts/sync_deletions.py $DELETED
          fi

      - name: Commit and push translations
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src/content/
          
          # Debug: show what is being committed
          git status
          
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore: auto-translate and sync content"
            git push
          fi

  verify:
    needs: translate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Pull latest changes
        run: |
          git pull origin ${{ github.head_ref }}
          git log -1 --oneline
          ls -R src/content/

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Verify content integrity
        run: python .github/scripts/verify_content.py
